apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-cloudijs-nl
  namespace: apps
spec:
  secretName: test-cloudijs-nl-cert
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-http
  dnsNames:
    - test.cloudijs.nl

