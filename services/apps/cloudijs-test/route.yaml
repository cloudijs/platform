apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cloudijs-test
  namespace: apps
spec:
  hostnames:
    - test.cloudijs.nl
  parentRefs:
    - name: shared-gateway-http
      namespace: cloudijs-system
  rules:
    - backendRefs:
        - name: cloudijs-test
          port: 80
